#pragma once

/*
 * HealthBarComponent
 *
 * Component that used to detect health bar wanted entities.
 * Currently empty, in a production ready engine this would had some useful variables.
 */
struct HealthBarComponent
{
public:
	HealthBarComponent() = default;
};

